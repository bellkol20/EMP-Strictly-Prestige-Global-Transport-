# Railway deploys the NestJS API from this monorepo (website stays on Vercel).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/main.js"
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/"
healthcheckTimeout = 30
